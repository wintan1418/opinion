<%= form_with(model: opinion) do |form| %>
  <% if opinion.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(opinion.errors.count, "error") %> prohibited this opinion from being saved:</h2>

      <ul>
        <% opinion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>
  <div class="form-group">
    <% if @opinion.image.attached? %>
    <%= image_tag @opinion.image, style: "width: 200px; display: block" %>
    <%end%>
    
    <%= form.label :image%>
    <%= form.file_field :image, class: "form-control"%>

  </div>


  <div>
    <%= form.submit %>
  </div>
<% end %>
